n = 5

def fact(n): #n немає відношення до n з першого рядка програми
  if n == 0: #це n яка використовується з функції і немає відношення до n з першого рядка
    return 1
  else:
    return n * fact(n-1)

print(str(fact(n))) #це змінна n з першого рядка програми
